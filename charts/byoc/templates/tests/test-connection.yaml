apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "byoc.fullname" . }}-test-connection"
  labels:
{{ include "byoc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "byoc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
